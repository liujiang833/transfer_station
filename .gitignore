# Built binaries (rebuild with ./build.sh)
/attn
/flash
/sanity
/bfdot_peak
*.o

# Vendored qemu-aarch64-static (~145 MB) -- fetch without root per README:
#   cd qemu_pkg && apt-get download qemu-user-static && dpkg-deb -x qemu-user-static_*.deb ./extracted
/qemu_pkg/

# Vendored python tooling (clang-format etc.)
/.pytools/

# Performance-modeling workspace (local analysis + generated .xlsx; not part of the kernel)
/modeling/
