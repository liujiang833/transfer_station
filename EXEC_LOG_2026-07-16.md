# Task Execution Log: SVE BFDOT Flash Attention Port

**Date:** 2026-07-16
**Goal:** Replace the flash-attention kernel in `attn_bf16_kernel/` with a fully-vectorized,
SVE-only BFDOT kernel adapted from `kutacc/src/attention/flash_mha/flash_attention.cpp`,
and benchmark 1k/2k/4k prefill at Qwen3-0.6B / 1.7B attention shape.

## Problems to Solve

1. Port kutacc's flash attention (SME/ZA-based) to a standalone SVE **BFDOT** kernel.
2. Fully vectorize: QK^T, online softmax, and P·V all in SVE — no scalar inner loops.
3. Use **only SVE** instructions: no NEON (`arm_neon.h`), no SME (`svmopa`/ZA/`__arm_streaming`).
4. Packing: **do not pack Q/K**; **pack V** to V^T. (Confirmed with user 2026-07-16.)
5. Benchmark 1k / 2k / 4k for Qwen3-0.6B and Qwen3-1.7B.

## Findings (established before implementation)

### F1. kutacc's kernel is SME, not SVE — and that is *why* it packs Q and K
`qk_gemm_4vl_1vl` / `pv_gemm_2vl_2vl_out_gemv` in `gemm.h` are `__arm_new("za") __arm_streaming`
and built on `svmopa_za32_bf16_m` (outer-product accumulate into the ZA tile). MOPA dictates
operand layout, so the kernel pre-packs Q, K **and** V
(`block_pack_bf16_1VL_trans_gemm_varlen`, `block_pack_bf16_2VL_gemm_varlen`).
BFDOT has no ZA-layout constraint, so that packing rationale does not carry over.

### F2. BFDOT decides the packing, and it agrees with the user's instruction
- `QK^T` contracts over `head_dim`, which is contiguous in **both** Q rows and K rows
  → feed `svld1_bf16` directly. **No Q/K pack.**
- `P·V` contracts over **keys**: contiguous in P, but stride-`D` in V
  → V must be transposed to `Vt[d][k]`. **Pack V only.**

Consequence (accepted): without a packed K, BFDOT cannot use the `svbfdot_lane` outer-product
form, so each score/output element ends in one `svaddv` horizontal reduction. Mitigate with
multiple independent accumulators (several j per pass) for ILP. This is the documented
"BFDOT is GEMV-friendly" path.

### F3. *** GCC 13 miscompiles `(bfloat16_t)float` casts — DO NOT USE THEM ***
`aarch64-linux-gnu-gcc 13.3.0`, `-march=armv8.6-a+sve+bf16`:

```c
bfloat16_t a[64];
for (int i = 0; i < 64; i++) a[i] = (bfloat16_t)1.0f;   /* -> bits 0x3c00 */
```
`0x3c00` is **IEEE fp16** 1.0. bfloat16 1.0 is **`0x3f80`**. The compiler applies fp16
conversion semantics to the cast. Reproduces identically at `-O0` and `-O2`, so it is not an
optimizer artifact. It is easy to miss because fp16 and bf16 share the encoding `0x4000`
for 2.0 — a probe using only 2.0 looks correct.

**Rule for this kernel:** never convert float→bf16 with a C cast. Use either
- the explicit `uint16_t` bit-pattern helper (`f32_to_bf16`, round-to-nearest-even), as
  `attn.c`/`flash.c` already do, or
- the SVE intrinsic `svcvt_bf16_x` (verified correct, see F4).

Repro: `scratchpad/probe3.c`.

### F4. The SVE path itself is sound under QEMU 8.2.2 `-cpu max` (VL=512 bits)
Verified against scalar references:
- `svbfdot_f32` on bit-pattern bf16: `64x(1.0*2.0) = 128.0` OK; `sum(1..64) = 2080.0` OK.
- `svcvt_bf16_x` + `svuzp1_bf16` (fp32→bf16, vectorized): matches the scalar RNE reference on
  all 32 lanes, 0 mismatches; round-trip through `svbfdot` exact.

So the vectorized fp32→bf16 softmax→P conversion is viable; only the C cast is broken.
Repro: `scratchpad/probe2.c`, `scratchpad/probe4.c`.

### F5. `fast_exp` is portable as-is
`kutacc/src/math/fast_exp.h` is pure SVE (`svexpa`-based exp2 polynomial), no SME.
Portable to C with only namespace/`auto` removal.

### F6. Qwen3-0.6B and Qwen3-1.7B have IDENTICAL attention shapes
Both: 16 Q heads / 8 KV heads (GQA group=2) / head_dim 128 / 28 layers.
Only `hidden_size` differs (1024 vs 2048), and that never reaches the attention kernel — it
only sizes the QKV projections. Benchmarking both therefore yields identical numbers.
Per user decision (2026-07-16): benchmark the shape **once**, label it as covering both models.

## Steps Log

### Step 1: Scope + verify toolchain foundation
- **Status:** success
- **Result location:** this file (F1–F6); probes in scratchpad `probe2.c` `probe3.c` `probe4.c`
- **Success result:** SVE+BF16 builds and runs under QEMU; BFDOT and `svcvt_bf16_x` verified
  correct; GCC 13 bf16-cast bug found and pinned down; Qwen3 configs confirmed identical.

### Step 2: Implement SVE BFDOT flash kernel (executor)
- **Status:** success
- **Result location:** `attn_bf16_kernel/flash.c`, `run.sh` (MARCH -> `armv8.6-a+sve+bf16`)
- **Success result:** flash.c rewritten SVE-only (`arm_sve.h` is the sole ARM header; no NEON,
  no SME). `svbfdot_f32` drives both QK^T and P.V. Q/K unpacked, V transposed to `Vt[d][k]`
  per F2. Softmax fully SVE via the ported `fast_exp` (no `expf` outside the fp32 reference).
  BFMMLA path + `use_mmla` flag dropped (NEON-only); harness no longer compares dot-vs-mma.
  Builds clean with `-Wall -Wextra` (zero warnings). Short suite: 9/9 PASS, err/scale ~1e-3.

### F7. No padding needed anywhere — predication subsumes it
The old NEON code zero-padded tiles (`Bqp` even, `Bkp`/`Dp` mult-8). SVE does not need this:
- D-tail: `svwhilelt_b16` zeroing load makes BFDOT's pair lanes contribute 0.
- Key-tail + causal mask: both are *prefixes* of j, so the entire mask collapses to a single
  `whilelt` bound `jmax = min(rk, lim-kj+1)`. No `-inf` fill, no padded tile.
This removed the Qp/Kp pack buffers and all `RUP()` padding.

### F8. VL-agnosticism verified on hardware-legal widths, not just VL=512
`qemu -cpu max,sve<N>=on,sve-default-vector-length=-1` for N = 128/256/512/1024/2048:
all five report the expected `svcntw()`/`svcnth()` and give ALL PASS. VL=2048 is the strong
case: `svcnth()`=128 exceeds `BK`=64, which is exactly what a padded design would break on.

### F9. The fp32 reference NaNs on fully-masked rows (pre-existing; harness blind spot)
If `Sk < Sq` and causal, early rows attend zero keys -> `attn_ref` does `0/0` -> NaN -> the
comparison `NaN > tol` is *false*, so those rows pass vacuously. The kernel emits 0.0 there
(`l=0 -> inv=0`), which is correct/standard. ~~**Does not affect the shipped suite:** every
causal case there has `Sk >= Sq`, so every row has >= 1 valid key.~~ ~~Noted, not fixed (the
reference is specified to stay as-is).~~

> ### ⚠ CORRECTION 2026-07-16 (see F14): the struck sentences above were **FALSE**.
> The shipped suite does **not** satisfy `Sk >= Sq` on every causal case. Counter-example, in
> the shipped short suite the whole time:
> ```c
> run_case("odd + multiblock", 6, 3, 130, 70, 40, 1, 0);  /* Sq=130, Sk=70, causal, ref_cap=0 */
> ```
> `Sk=70 < Sq=130`, so `lim(i) = Sk-Sq+i = i-60` is negative for rows 0..59: those 60 rows per
> head attend **zero** keys. With `Hq=6` and `ref_cap=0` (every row checked), **360 of 780
> checked rows (46%) were vacuous NaN passes** — measured with an instrumented build, not
> deduced. This was the worst case to lose: `odd + multiblock` is the **only** case in the short
> suite spanning >1 query block **and** >1 key block, i.e. the sole short-suite check of the
> multi-block online-softmax path. The claim "not fixed / reference stays as-is" is also
> superseded: **fixed in F14** (reference emits a zero row; comparison made NaN-safe).

### Step 3: Independent verification (verifier)
- **Status:** success
- **Result location:** `flash_long.log`; stress variant in scratchpad `flash_stress.c`
- **Success result:** beyond the 9 shipped cases, 13 extra stress shapes (odd D = 1/3/7/33/65,
  block edges Sq/Sk = 63/64/65, Sq=1, Sk<Sq causal, MQA g16) x 3 VLs = 39 combos, all PASS.

### Step 4: Qwen3 1k/2k/4k benchmark
- **Status:** success
- **Result location:** `flash_long.log` (full output; 19m05s total)
- **Success result:** Hq=16 Hkv=8 (g2) D=128 causal, all PASS vs the fp32 ref (ref_cap=8 rows):

| Sq=Sk | useful GFLOP (causal) | QEMU s | GFLOP/s | err/scale |
|-------|----------------------|--------|---------|-----------|
| 1024  | 4.30                 | 54.52  | 0.079   | 1.4e-03   |
| 2048  | 17.19                | 217.66 | 0.079   | 1.2e-03   |
| 4096  | 68.74                | 869.70 | 0.079   | 1.6e-03   |

- Time scales 3.99x / 4.00x per doubling — clean O(S^2); the causal block-skip holds.
- Useful (causal) flops = `4*Hq*D*#{(i,j): j<=i}` = **exactly half** (ratio 2.000, verified) of
  the non-causal `4*Hq*Sq*Sk*D` the old harness printed.
- 0.079 GFLOP/s is QEMU **functional emulation** throughput, NOT ARM hardware speed.
- Benchmark binary proven byte-identical to a build of the final shipped source (`cmp`), so
  the timings apply to the delivered `flash.c` exactly.

---

## Addendum 2026-07-16 (later): harness hardening — tolerance + reference row selection

**Goal:** fix two *test-harness* weaknesses found after the kernel was verified. Kernel compute
was NOT touched (proved below); only `attn_ref` and the harness changed.

### Problems to Solve (round 2)

6. The pass tolerance (`atol = rtol = 2e-2`) was ~30x looser than the real bf16 noise, so a
   uniform 1% output error passed all 9 cases.
7. The qwen3 1k/2k/4k check used `ref_cap=8` as a **prefix**; since those cases are causal with
   `Sq == Sk`, rows 0–7 attend only keys 0–7 — an 8x8 corner of the FIRST key block. The
   multi-block online-softmax rescale was never validated at 1k/2k/4k.

### F10. Real bf16 noise measured: worst `err/scale` = 2.76e-3 across 129 shapes x 2 VLs

Instrumented copy — **saved as `tolerance_probe.c`** (Rule 2; the earlier steps' probes lived in
an ephemeral scratchpad and are now lost, so this one is kept in-tree; it has build/run lines in
its header) — printed `mad/maxref` for a sweep of
D = 1/2/3/7/8/16/33/40/64/65/96/128, Sq/Sk block edges 63/64/65/127/128/129 (all 36 pairs),
`Sk < Sq` causal, GQA g = 1/2/4/16, decode Sq=1 with Sk up to 1024, full-reference multi-block
causal at 256/384/512, causal + non-causal — at **VL=512 and VL=2048**.

| statistic | value |
|-----------|-------|
| n cases | 129 (x2 VLs) |
| median | 1.57e-03 |
| p90 | 2.17e-03 |
| **max** | **2.76e-03** (`D=8`, non-causal) |
| the shipped 9 | <= 1.70e-03 |

VL=512 vs VL=2048 were **numerically identical** except one case in the 3rd digit
(`D=3 full`, head-norm 2.80e-3 vs 2.81e-3). Worst case identical to 4 digits. So the noise is
VL-invariant and the tolerance needs no VL margin.

Why the non-causal cases dominate: the error is bounded by `2^-9 * max_j|V[j][d]|` (bf16 P), so
it is set by `max|V| ~= 1.0` regardless of shape. For **causal**, `maxref ~= 1.0` too (row 0
attends one key, so `O[0] = V[0]` exactly), and `err/scale ~= 2^-9`. For **non-causal**, the
softmax averages ~96 random V's so `maxref` falls to 0.12–0.24 while the error does not — the
denominator shrinks, and `err/scale` rises. Non-causal is the conservative direction.

### F11. Per-ROW and per-HEAD normalisation are degenerate at small D — per-CASE is the robust one

Measured all three on the same data:

| normaliser | worst on a CORRECT kernel | verdict |
|-----------|---------------------------|---------|
| per-row `mad_i / max_d abs(O[i][d])` | **1.159** (`D=1` causal) | **rejected** |
| per-head | 5.60e-03 (`D=1` full) | rejected |
| per-case | 2.76e-03 (`D=8` full) | **chosen** |

At `D=1` a row's scale is a *single* element; when it cancels to ~0 the relative error explodes
on a correct kernel. Per-head fails the same way, more mildly: the offending `D=1` head had
`maxref=0.116` while its absolute error was a perfectly normal 6.5e-4 — the *denominator* was
the anomaly, not the numerator. Pooling the whole case makes the scale estimate robust.

### F12. **The prefix check was blind to a dropped key block — spread rows catch it**

Direct A/B, same data, same tolerance, kernel deliberately broken with
`if (kj == BK) continue;` (silently drop the 2nd key block), qwen3 shape Sq=Sk=512 causal:

| reference rows | err/scale | verdict |
|----------------|-----------|---------|
| prefix 0..7 (OLD) | 1.3e-03 | **PASS** — indistinguishable from a correct kernel |
| spread 0..511 (NEW) | **1.5e-01** | **FAIL** — 19x over the bound |

This is the concrete justification for Problem 7: the old harness would ship a kernel that
silently drops an entire key block. Rows 0–7 never reach block 1, so no tolerance however tight
could have caught it — it was a *coverage* bug, not a tolerance bug.

### F13. `err/scale` is diluted ~7x on causal spread-row checks (known, accepted limit)

For causal `Sq==Sk`, `maxref ~= 1.0` comes from row 0 (`O = V[0]`, zero error), while the error
comes from the late rows whose own scale is only ~0.13. Measured at qwen3 1k with spread rows:
per-case `err/scale = 2.92e-04` but per-row `= 2.20e-03` — a ~7.5x gap. So a *subtle* (~1%)
drift confined to late rows is under-reported by ~7x at the long shapes and would need to reach
~5–6% of the row's own magnitude to trip TOL. **Structural** multi-block bugs are far above that
(F12 measured 1.5e-01) and are caught loudly. Per-row normalisation would close this gap but is
unusable (F11). Accepted and documented; the natural follow-up, if ever needed, is a per-row
bound with a floor tied to the case scale.

### Tolerance chosen: `TOL = 8e-3`, criterion `mad <= TOL * maxref`

Replaces `|k-r| <= atol + rtol*|r|` (atol=rtol=2e-2). The old form mixed an absolute term that
**dominated** — against outputs of O(0.1–1.0) it evaluated to an effective ~3e-2 bound, ~30x the
real ~1e-3 noise. The new criterion is normalised by the per-case output scale, so **the printed
`err/scale` IS the pass metric** — headroom is readable straight off the output line.

Margins: **2.9x** over the global measured worst (2.76e-3), **4.7x** over the shipped suite's
worst (1.70e-3), ~27x over the qwen3 long cases.

**Honest note on the "~4-5x" target:** 4-5x over the *global* worst would be 1.10e-2–1.38e-2,
which is **above 1e-2 and so would NOT catch a clean x1.01** — a uniform scale error `e` lands
`err/scale ~= e` (the max-|ref| element carries the max absolute error). The sensitivity
requirement therefore **caps the usable margin at ~3.5x**; the two requirements are in tension
and 8e-3 is the balance. It is 4.7x over what the suite it actually guards exhibits.

### Sensitivity + stability, both measured

- **Sensitive:** uniform x1.01 output scale injected into a /tmp copy → **all 9 cases FAIL**
  (not merely the suite), exit 1. Bisected against the FINAL shipped source:
  **x1.0072 PASS / x1.0073 FAIL → ~0.73%**, beating the ~1% target. Symmetric: x0.9925
  (0.75% down) also caught. (The in-source comment quotes the wider x1.0072/x1.0074 bracket
  from the first bisection pass — still true, just not the tightest.)
- **A/B vs the old bound** (same rows/data, only the criterion swapped): old detects **~3.5–4%**
  (x1.035 PASS, x1.04 FAIL), new detects **0.74%** → **~4.7x sensitivity gain**.
- **Stable:** unmodified kernel PASSes 9/9 at **VL = 128/256/512/1024/2048**, worst `err/scale`
  = 1.7e-03 at every width → **4.7x headroom**, zero flakiness, exit 0 everywhere.

### Kernel compute proved untouched

`scratchpad/flash_probe.c` was created as `head -389 flash.c` from the **pre-edit** source, so its
kernel region is a verbatim pre-edit copy. Source diff of the current `flash.c` against it:
`fast_exp` region **identical**; compute tiles + `attn_flash` region **identical** (sole diff was
one trailing blank line from the extraction boundary). Note: an objdump symbol diff is *useless*
here — at -O2 every kernel function inlines into `run_case`, leaving no symbols to compare (a
naive attempt produced two empty dumps and a vacuous "identical").

### Step 5: harness hardening (tolerance + spread rows)
- **Status:** success
- **Result location:** `flash.c` (`attn_ref`, new `ref_rowset`, `TOL`, `run_case`);
  measurement harness saved in-tree as `tolerance_probe.c` (regenerates the sweep in ~70s/VL)
- **Changes:** `attn_ref` now takes `(const int *rows, int nrows)` instead of a `ref_rows`
  prefix count and writes only those rows; new `ref_rowset()` spreads `ref_cap` rows evenly over
  `[0,Sq)` with `rows[0]=0` and `rows[cap-1]=Sq-1`; `Oref`/`Odot` are `calloc`'d and the
  comparison walks the same `rows[]`, so uninitialised reference memory is never read; criterion
  is now `mad <= TOL*maxref` judged once after `mad`/`maxref` are final; label `[ref:subset]` →
  `[ref:N rows spread 0..Sq-1]`. Header comment updated for the changed semantics.
- **Builds warning-free** under `-Wall -Wextra`. Short suite 9/9 PASS in 0.196s.

### Step 6: full re-verification after the harness change
- **Status:** success
- **Result location:** this file; `./flash` and `./flash --long` re-run against the final source
- **Short suite:** 9/9 PASS in **0.199s** (unchanged speed). The 9 `err/scale` values are
  **bit-identical to the pre-change run** (1.4e-3, 9.4e-4, 1.5e-3, 1.3e-3, 1.1e-3, 1.7e-3,
  1.6e-3, 1.3e-3, 1.5e-3) — those cases use `ref_cap=0` (full reference, every row), so the row
  change is a no-op for them and only the criterion changed. Verdicts unchanged.
- **`--long`:** **ALL PASS**, 18m52s, exit 0. Benchmark binary md5 `e4688d23...` proven equal to
  a fresh build of the shipped source, so the numbers apply to the delivered `flash.c` exactly.

| Sq=Sk | rows checked (spread) | key blocks row Sq-1 walks | err/scale, prefix (old) | err/scale, spread (new) | headroom vs TOL | verdict |
|-------|----------------------|---------------------------|------------------------|-------------------------|-----------------|---------|
| 1024  | 0,146,292,438,584,730,876,**1023** | 16 | 1.4e-03 | 2.9e-04 | 27x | PASS |
| 2048  | 0,292,584,877,1169,1462,1754,**2047** | 32 | 1.2e-03 | 2.1e-04 | 38x | PASS |
| 4096  | 0,585,1170,1755,2340,2925,3510,**4095** | **64** | 1.6e-03 | 1.5e-04 | 53x | PASS |

**No verdict changed** — 1k/2k/4k still PASS, now for a real reason: row Sq-1 walks every key
block (64 of them at 4k) and every rescale, where the old prefix check never left key block 0.

**Do NOT read the lower spread err/scale as better accuracy** — it is the F13 dilution: the late
rows carry the error but their own scale is ~0.13, while `maxref ~= 1.0` still comes from row 0
(`O = V[0]`, zero error). The *absolute* error is unchanged; the denominator is not.

Timings match Step 4 to ~1% (56.70 / 216.03 / 856.38s vs 54.52 / 217.66 / 869.70s) — the spread
rows cost no measurable extra reference work, as predicted (`attn_ref` is O(Sk*D) per row
wherever the row sits).

### Residual limits (known, not fixed — out of scope for this change)
- **F13 dilution** (~7x on causal spread-row checks): a subtle ~1% drift confined to late rows is
  under-reported at the long shapes. Structural multi-block bugs are caught loudly (F12: 1.5e-01).
- **F9 NaN blind spot** is worse than Step 3 recorded: the probe counted the vacuous rows.
  `Sk<Sq` causal 200x33 has **668/800 checked rows NaN (83%)**, 130x70 has 360/780 (46%). Those
  rows pass vacuously (`NaN > tol` is false; NaN also never updates `mad`/`maxref`). Pre-existing,
  unchanged by this work, and ~~still absent from the shipped suite (every shipped causal case has
  `Sk >= Sq`). Worth fixing if `Sk<Sq` causal ever ships.~~

  > **⚠ CORRECTED 2026-07-16 (F14): the struck clause was FALSE, and self-contradictory.**
  > `Sk<Sq` causal was **already shipped** — `130x70` *is* the shipped `odd + multiblock` case
  > (`run_case("odd + multiblock", 6, 3, 130, 70, 40, 1, 0)`). This very bullet quotes that
  > shape's own vacuous-row count (`360/780`) while calling the shape absent from the suite; the
  > "absent" clause was simply wrong, and the `if ... ever ships` condition was already met.
  > **Fixed in F14**, not deferred.
- `flash.c` has 8 pre-existing `clang-format` violations (lines 65-67, 304, 311, 552-554) under
  the bundled clang-format 22.1.8. None are in the code this change added; two are inside the
  kernel, which is off-limits. Left alone.

---

## Addendum 2026-07-16 (later still): F9 fixed — the NaN blind spot was LIVE in the shipped suite

**Goal:** close the F9 blind spot, which the two corrections above show was **not** hypothetical
and **not** confined to unshipped shapes. Kernel compute NOT touched (proved below); only
`attn_ref`'s normalise step and `run_case`'s comparison changed.

### F14. F9 was live on `odd + multiblock`: 360/780 rows (46%) were never actually compared

**The earlier claim that "every shipped causal case has `Sk >= Sq`" was wrong** (corrected in
place at F9 and at the Residual-limits bullet). `odd + multiblock` is `Sq=130, Sk=70, causal`,
so `lim(i) = i-60 < 0` for rows 0..59 → 60 fully-masked rows/head × `Hq=6` = **360 of 780**
checked rows. `attn_ref` divided by `sum==0` there, giving `0/0 = NaN` across the whole row;
`NaN > tol` is false, so those rows passed **vacuously** — and NaN never updated `mad`/`maxref`
either, so nothing in the printed output hinted at it.

Severity: `odd + multiblock` is the **only** short-suite case spanning >1 query block *and* >1
key block. The one case guarding the multi-block online-softmax path was the one case that was
46% unchecked.

**Measured, instrumented build (`scratchpad/flash_nan_before.c` / `_after.c`), NaN reference rows:**

| case | before | after |
|------|--------|-------|
| MHA prefill / prefil.causal / GQA ×3 / MQA / decode ×2 / big head_dim | 0 | 0 |
| **odd + multiblock** | **360/780 (46.2%)** | **0/780 (0.0%)** |
| **TOTAL over the short suite** | **360/1080** | **0/1080** |

### The two fixes

1. **`attn_ref`: emit a zero row, not NaN, for a fully-masked row.** `s[j] /= sum` →
   `float inv = (sum > 0) ? 1.0f/sum : 0.0f; s[j] *= inv;`. This mirrors the kernel's
   `l==0 -> inv=0` exactly, and matches standard flash-attention. It cannot misfire on a real
   row: with ≥1 valid key the argmax term contributes `expf(0)=1`, so `sum >= 1` strictly.
2. **`run_case`: NaN-safe comparison.** Non-finite elements are counted **out-of-band**
   (`long nbad`) and fail the case unconditionally: `int ok = (nbad == 0) && (mad <= TOL*maxref)`.
   Out-of-band is deliberate — a NaN cannot be carried *in* `mad`, because `!(ad <= mad)` would
   latch NaN and then the **next finite `ad` would overwrite it** (poison, then un-poison). The
   final test keeps the `mad <= bound` form (fails closed on NaN) rather than `!(mad > bound)`.
   The verdict names the count (`*** FAIL (N non-finite) ***`) because `err/scale`, computed from
   the finite remainder, looks perfectly innocent when NaN is the bug — see the A/B below.

### F15. The 360 rows now pass for a REAL reason, and that is why `err/scale` did not move

Instrumented split of `odd + multiblock`'s checked rows (`scratchpad/flash_split.c`):

| row group | rows | elements | ref==0.0 AND kernel==0.0 exactly | max\|dev\| | max\|ref\| |
|-----------|------|----------|----------------------------------|-----------|-----------|
| fully-masked (`lim<0`) | 360 | 14400 | **14400/14400 (100%)** | 0.0 | 0.0 |
| normal | 420 | 16800 | — | 1.296043e-03 | 9.882812e-01 |

So the 360 rows are now a genuine kernel-0.0 vs ref-0.0 equality check, not a skip.

**`err/scale` for `odd + multiblock` is UNCHANGED at 1.3e-03** (before: 1.3e-03, after: 1.3e-03).
This was worth checking rather than assuming, and the reason it held is exact: the masked rows
contribute `|dev| = 0` (never raises `mad`) **and** `|ref| = 0` (never raises `maxref`) — a zero
can no more raise a maximum than a NaN could. **The `maxref` denominator did not change**: it is
`0.9883`, set by the normal rows, both before and after. Hence
`err/scale = 1.296043e-03 / 9.882812e-01 = 1.31e-03` → prints `1.3e-03` either way. The 360 rows
went from *skipped* to *verified*, with no effect on the metric — the ideal outcome, since it
means the fix adds coverage without perturbing the tolerance story (F10–F13 all still hold).

### F16. The guard has teeth: A/B fault injection (this is what would have caught F9)

One NaN forced into the **kernel output** on a checked element (`Odot[(0*Sq+rows[0])*D+0] = z/z`,
`z` a volatile 0.0f), injected in `run_case` *after* `attn_flash` returns — so even the probe
leaves kernel compute untouched. Same injection, same data, only the harness swapped:

| harness | result | exit |
|---------|--------|------|
| **OLD** (pre-fix comparison) | **ALL PASS — all 9 cases, NaN completely undetected** | **0** |
| **NEW** (post-fix comparison) | **SOME FAILED — all 9 report `*** FAIL (1 non-finite) ***`** | **1** |

The old harness ships a kernel emitting NaN and says `=== ALL PASS ===`. Note the new run still
prints `err/scale=1.4e-03` etc. on the failing lines — the scale metric is blind to NaN by
construction, which is why the verdict, not the metric, carries the signal.

### F17. The fix generalises: 8 `Sk<Sq` causal shapes, 0 NaN rows, all PASS

Beyond the shipped suite (`scratchpad/flash_stress_masked.c`, instrumented), every shape that
produces fully-masked rows now has **zero** NaN reference rows and PASSes:

| shape (`Sq x Sk`, causal) | masked rows | NaN ref rows after | err/scale | verdict |
|---------------------------|-------------|--------------------|-----------|---------|
| 200x33 (`Hq=4`) | 668/800 (83%) | 0/800 | 9.5e-04 | PASS |
| 130x70 (`Hq=6`, the shipped case) | 360/780 (46%) | 0/780 | 1.3e-03 | PASS |
| 64x1 | 126/128 (98%) | 0/128 | 0.0e+00 | PASS |
| 129x1 | 256/258 (99%) | 0/258 | 0.0e+00 | PASS |
| 65x64, 300x64, 128x127, 100x20 (D=1) | — | 0 | 7.8e-04 … 1.1e-03 | PASS |

Two cross-checks worth recording:
- **200x33 reproduces 668 masked rows exactly**, matching the count F9's earlier probe measured
  independently (`668/800, 83%`). Two separate instrumentations agree.
- **`err/scale = 0.0e+00` on `64x1`/`129x1` is NOT a degenerate `maxref==0`** — checked, not
  assumed, since the printf renders both as `0.0e+00`. `maxref` is 0.875 / 0.988 (from the 2
  non-masked rows) and `mad` is exactly 0. That is correct and expected: with `Sk=1` the single
  attending row has a one-element softmax = exactly 1.0, so `O = V[0]` bit-exactly. 98–99% of
  those rows are fully masked and are now all verified zero-vs-zero.

### Kernel compute proved untouched (again)

Pre-edit `flash.c` snapshotted to `scratchpad/flash_pre.c` (md5 `62bac93b3b39b0df787ee27987a4907d`,
the exact md5 the previous `flash_long.log` header cites) before any edit. Region diff of
`fast_exp*` + `pack_vt` + `qk_tile` + `pv_acc` + `softmax_block` + `attn_flash`, pre vs post:
**identical, zero lines differ** (14397 bytes, md5 `35c8e05b57062de0222bb09fcc99c261` on both).
Per-function md5s also compared individually — all 8 identical. The complete diff is 6 changed
statements, all inside `attn_ref`'s normalise and `run_case`'s comparison, plus comments.

### Step 7: fix F9 (zero row + NaN-safe comparison)
- **Status:** success
- **Result location:** `flash.c` (`attn_ref` normalise guard; `run_case` `nbad` + verdict);
  `flash_long.log` regenerated; probes in scratchpad (`flash_nan_before.c`, `flash_nan_after.c`,
  `flash_split.c`, `flash_inject_OLD.c`, `flash_inject_NEW.c`)
- **Builds warning-free** under `-Wall -Wextra` (0 warning lines).
- **Short suite: 9/9 PASS in 0.21s** (unchanged; was 0.196–0.199s). All 9 `err/scale` values
  **bit-identical** to the pre-fix run: 1.4e-3, 9.4e-4, 1.5e-3, 1.3e-3, 1.1e-3, 1.7e-3, 1.6e-3,
  **1.3e-3 (odd + multiblock)**, 1.5e-3. No verdict changed.
- New `flash.c` md5 `55536a6b5f950298a29f7697213ae8ab` (was `62bac93b3b39b0df787ee27987a4907d`),
  so `flash_long.log` was regenerated — its header cites the md5.

### Step 8: `--long` re-run + `flash_long.log` regenerated
- **Status:** success
- **Result location:** `flash_long.log` (header now cites md5 `55536a6b…`; format reproduced
  exactly — 34 lines, blank lines at 4 and 31, same title/`# regenerated`/`# host` structure)
- **`--long`: ALL PASS, exit 0, 19m13.843s.** Binary proven byte-identical to a fresh build of
  the shipped source (`cmp`), so the numbers apply to the delivered `flash.c` exactly.
- **Long numbers VERIFIED unchanged, not assumed.** The `--long` cases are causal with `Sq==Sk`,
  so `lim(i) = i >= 0` for every row — no fully-masked rows, hence the fix cannot reach them.
  Confirmed by machine diff of old vs new log across **all 12 cases**: `err/scale`, GFLOP and
  verdict are **identical on every one**.

| case | err/scale old → new | GFLOP | QEMU s old → new | Δt |
|------|---------------------|-------|------------------|-----|
| qwen3 1k | 2.9e-04 → **2.9e-04** | 4.30 (same) | 55.00 → 54.56 | −0.8% |
| qwen3 2k | 2.1e-04 → **2.1e-04** | 17.19 (same) | 216.76 → 217.24 | +0.2% |
| qwen3 4k | 1.5e-04 → **1.5e-04** | 68.74 (same) | 867.24 → 878.64 | +1.3% |
| the 9 short cases | all identical | same | — | — |

The **only** content diffs in the whole log are: the header date+md5 (intended), the wall-clock
times above, and the `GFLOP/s` column on the sub-0.01s short cases (a rate computed from a
~0-second denominator — pure quantisation noise). **No number moved materially.** The ±1.3% on
4k is run-to-run QEMU variance, not a change: Step 6 already recorded 856.38s vs Step 4's 869.70s
(1.5%) for *identical* source, so ±1.3% sits inside the established band — and `err/scale`, the
actual pass metric, is bit-identical regardless.

## Addendum 2026-07-17: `attn_flash` made ALLOCATION-FREE (caller-owned scratch)

**Goal:** the kernel must not allocate. A driver passes one scratch buffer; `attn_flash`
mallocs nothing, so it cannot fail for want of memory and can be called under a bump
allocator or per-thread without touching the heap.

### Problems to Solve (round 3)
1. `attn_flash` malloc'd 8 buffers per call and freed them at the end — heap traffic on
   every call, and an allocation failure path in a compute kernel.
2. A size query and a separate carve are the classic silent-heap-overflow pair. They must
   not be able to drift.
3. The sub-buffers mix `uint16_t` and `float` and their extents depend on `D`, so without
   padding each sub-buffer's start would wander with `D`.

### The API
```c
size_t attn_flash_scratch_bytes(int D);                       /* size query          */
static size_t attn_flash_layout(void *base, int D, struct attn_scratch *s); /* THE layout */
static void attn_flash(..., int causal, void *scratch);       /* + scratch parameter */
#define ATTN_SCRATCH_ALIGN 64                                 /* documented contract */
```

### F18. One layout function is the single source of truth — by construction, not by assertion
`attn_flash_layout(base, D, s)` both measures (`base == NULL`) and carves (`base != NULL`).
`attn_flash_scratch_bytes(D)` **is** its measuring pass (`return attn_flash_layout(NULL, D,
NULL);`) and `attn_flash` calls it to carve. The sizes exist in exactly one place, so the
reported size cannot drift from the carve — there is no second sum to keep in step. Each
sub-buffer's offset is padded up to `ATTN_SCRATCH_ALIGN`=64 by a shared `attn_carve()`
helper, and the total is rounded to 64 (also making it a legal C11 `aligned_alloc` size).

### F19. The sizing is exact and non-overlapping — demonstrated, not asserted
`scratchpad/layout_check.c` (`#include`s the REAL flash.c, so it cannot test a stale copy)
carves at `D` ∈ {1,3,7,8,33,40,64,65,128,256} and checks all 8 sub-buffers against
independently hand-written extents. **All 10 D: in bounds, 64-byte aligned, pairwise
non-overlapping, measure == carve.** Reported size equals the carve's high-water mark
**exactly — slack 0 at every D**. Padding overhead ≤ 60 bytes (worst `D=1`, 0.23%); at
D=64/128/256 the extents are already multiples of 64 → padding exactly 0.
Sizes: D=1 → 25 792 B, D=40 → 40 896 B, D=64 → 50 176 B, D=128 → 75 008 B, D=256 → 124 672 B.
**Independent of `Sq`/`Sk`** — structural: the layout takes only `D`.

### F20. Zero allocation proved from the BINARY, and the proof tool was wrong twice first
At `-O2` `attn_flash` is `static` and inlines into `run_case`, so **no `attn_flash` symbol
survives in `./flash`** — and `run_case` legitimately mallocs (Q/K/V/Oref/Odot/rows/scratch),
so an inlined kernel cannot be separated from its caller by symbol. `scratchpad/callgraph_probe.c`
pins it: a non-static wrapper whose whole body is the call, which makes GCC emit a real
out-of-line `attn_flash`. Inlining can only ever *remove* calls, never invent a malloc absent
from the source, so a clean graph there is conclusive.

Two tool bugs found and fixed *before* trusting the result — both would have produced a
false PASS:
1. **Tail calls are edges.** `probe_attn_flash` compiles to a single `b <attn_flash>`; a
   `bl`-only regex called it a leaf that "calls nothing". Fixed to follow `b <sym>`.
2. **Allocators must match by stem.** In a static link the symbol is `__libc_malloc`, not
   `malloc`; an exact-name list silently missed it (and `__mmap64`).

Result (`callgraph.py`, ship flags): `attn_flash` direct callees = **`__sqrtf`,
`attn_flash_layout`, `__stack_chk_fail`** — no allocator. The only allocation-like symbols in
the transitive closure are `__mmap64`/`__munmap` reached *only* via
`__stack_chk_fail → __fortify_fail → __libc_message_impl`, i.e. glibc's stack-smash **abort**
reporter, unreachable in normal operation. Rebuilt with `-fno-stack-protector` to remove that
noise: closure is **3 symbols** (`attn_flash_layout`, `__sqrtf`, `__ieee754_sqrtf`),
**allocators NONE**, indirect calls 0. Stack frame is a **constant** `#0x240` with **no dynamic
sp adjustment** → no VLA/alloca. Negative control: the same tool on `flash_main_unused` finds
`run_case → __calloc`, `→ __free`, `→ aligned_alloc` — it does detect real allocator edges.

**Quantitative confirmation on the REAL shipped `./flash`** (allocator call sites inside
`run_case`, into which `attn_flash` and `attn_ref` are inlined):

| | `__libc_malloc` | `__free` | `__calloc` | `aligned_alloc` |
|---|---|---|---|---|
| HEAD `df5736b` | 13 | 15 | 2 | 0 |
| new | **5** | **8** | 2 | **1** |

Reconciles exactly: **−8 malloc and −8 free** (precisely `attn_flash`'s eight), **+1
`aligned_alloc` +1 free** (the harness's scratch). HEAD's 13 = 1 (`attn_ref`) + 4 (`run_case`)
+ 8 (kernel); new's 5 = 1 + 4 + **0**.

### F21. Canary + poison: no overflow, no uninitialised read — and both guards have teeth
`scratchpad/canary_probe.c` runs the **whole short suite** with the scratch poisoned to
`0xFF` (NaN in every f32/bf16 lane) immediately before each call, and a 256-byte `0x5A`
canary past `attn_flash_scratch_bytes(D)`, verified after. (Method: pre-include the real
headers, then macro-intercept `aligned_alloc`/`free` and pull in the real `flash.c` —
pre-including keeps the macros off the libc declarations. `run_case` allocates once and calls
once, so poison-at-allocation IS poison-before-the-call.)

- **Clean: 9/9 canaries intact, 0 breaches; 9/9 cases PASS with `err/scale` bit-identical to
  HEAD despite the NaN fill** → the kernel initialises everything it reads.
- **Teeth A (canary):** injecting a single 1-byte write at offset `scratch_bytes(D)` →
  **BREACHES: 9/9**. Note the suite still printed `ALL PASS` under that fault — a heap
  overflow is numerically invisible, which is exactly why the canary is the test that matters.
- **Teeth B (poison):** deleting `memset(acc, 0, ...)` → `*** FAIL (4096 non-finite) ***`.
  The NaN fill propagates to `O` and the harness's out-of-band non-finite counter (F14's fix)
  catches it.

### F22. The shipped suite never runs a head_dim whose padding shifts the carve — swept separately
Gap found while checking coverage: the 9 shipped cases use `D` = 40/64/128, whose carve padding
is **32/0/0**. The `D` where alignment padding actually shifts every later sub-buffer's offset
(1/3/7/33/65 → 60/52/36/60/60 bytes) get bounds+overlap checking from `layout_check.c` but no
**numerical** check. A padding bug that kept every buffer in bounds while subtly overlapping two
of them could therefore hide from the shipped suite.

Closed with `scratchpad/dsweep_probe.c`: real `attn_flash` vs the fp32 reference at
`D` ∈ {1,2,3,7,8,33,40,64,65,127,128,129,256}, two shapes each (`Hq=6/Hkv=3, Sq=130, Sk=70,
causal` — multi-block in both Sq and Sk with `Sk<Sq`; and `Hq=4/Hkv=2, Sq=8, Sk=8, full`), all
under the same poison+canary guards. **26/26 PASS, 0 canary breaches**, worst `err/scale` 3.0e-3
(`D=1`, well under `TOL`=8e-3). Then re-ran the identical 26 cases against **HEAD's** malloc-ing
kernel (`scratchpad/headref/`): `err/scale` **bit-identical on all 26**, D=1..256. So the refactor
is numerically inert far beyond the 9 shipped shapes.

### Kernel compute proved untouched (third time)
Pre-edit snapshot `scratchpad/flash_pre.c`, md5 `55536a6b5f950298a29f7697213ae8ab` (the md5
`flash_long.log`'s header cites). Per-function md5, HEAD vs new: `fast_exp2_sve`,
`fast_exp_sve`, `fast_exp_lane0`, `pack_vt`, `qk_tile`, `pv_acc`, `softmax_block` — **all 7
IDENTICAL**. `attn_flash`'s entire compute body (the `h`/`qi`/`kj` loop nest) is
**BYTE-IDENTICAL: 2047 bytes, md5 `a13924a6dd2e337e9549ca63353e0545` on both**. Achieved by
re-binding the same local names (`uint16_t *Vt = s.Vt, *Pb = s.Pb; float *S = s.S, ...`) so
not one line of compute moved. Whole-file code-only diff = 14 lines removed / 38 added, all of
it: the new layout block, the 8 mallocs → pointer plumbing, the 8 frees deleted, the signature
`+ void *scratch`, and the harness's `aligned_alloc`/`free`.

### Step 9: allocation-free scratch refactor
- **Status:** success
- **Result location:** `flash.c` (scratch section + `attn_flash` + `run_case`); `README.md`
  ("Allocation-free: the caller owns the scratch"); probes in `scratchpad/`
  (`layout_check.c`, `canary_probe.c`, `callgraph_probe.c`, `callgraph.py`, `faultA/`, `faultB/`)
- **Builds warning-free** under `-Wall -Wextra` (0 warning lines).
- **Short suite: 9/9 PASS in 0.19s.** A/B timed back-to-back, 3 runs each: HEAD 0.19/0.20/0.20s,
  new 0.19/0.20/0.19s — no change (the removed per-call mallocs were never the cost).
  All 9 `err/scale` **bit-identical**
  to HEAD, machine-diffed: 1.4e-3, 9.4e-4, 1.5e-3, 1.3e-3, 1.1e-3, 1.7e-3, 1.6e-3, 1.3e-3,
  1.5e-3. No verdict changed. Expected — this is a pure allocation refactor; the compute body
  is byte-identical, and `malloc` vs a carve of `aligned_alloc` memory changes no arithmetic.
- New `flash.c` md5 `78d9a6fe186d11cca5cc03378f4ce6ee`.
- `tolerance_probe.c` **untouched and still builds warning-free** — it carries its own frozen
  2026-07-16 snapshot of the kernel (old signature, own mallocs), so the API change cannot
  reach it. Deliberately left alone.

### Step 10: `--long` re-run + `flash_long.log` regenerated
- **Status:** success
- **Result location:** `flash_long.log` (header cites the new md5 `78d9a6fe…`; format reproduced
  exactly — 34 lines, blanks at 4 and 31)
- **`--long`: ALL PASS, exit 0, 18m45.4s.** The binary under test was proven byte-identical
  (`cmp`) to a fresh build of the delivered `flash.c`, so the numbers apply to it exactly.
- **All 12 cases machine-diffed against the previous log: `err/scale`, GFLOP and verdict
  IDENTICAL on every one** (qwen3 1k 2.9e-04, 2k 2.1e-04, 4k 1.5e-04). Expected: the compute
  body is byte-identical, and swapping 8 `malloc`s for a carve of caller memory changes no
  arithmetic.
- Wall clock (QEMU emulation, not ARM perf): 1k 54.56→54.80s, 2k 217.24→214.24s, 4k
  878.64→**853.06s** (−2.9%), total 19m13.8→18m45.4s. Run-to-run variance, not a speedup — the
  4k case has previously moved 869.70→856.38→878.64s across runs of *identical* source, and this
  run overlapped some A/B timing work. `err/scale`, the actual pass metric, is bit-identical.

### Residual notes (round 3)
- The canary+poison guards live in `scratchpad/canary_probe.c`, **not** in the shipped
  `run_case`: the task's own requirement is that `run_case` demonstrate the intended *driver*
  pattern, and a driver does not over-allocate a canary or poison its scratch. The shipped
  harness therefore does the clean thing (`aligned_alloc(ATTN_SCRATCH_ALIGN, sbytes)` → pass →
  `free`), and the guards are a probe that `#include`s the real `flash.c` so it can never test a
  stale copy.
- `attn_flash_scratch_bytes()` is deliberately **non-`static`** (per the required API) while
  everything else in the file stays static: it is the one symbol a driver links against. It is
  the only exported symbol; `-Wall -Wextra` does not enable `-Wmissing-prototypes`, so this is
  warning-free.
- The returned size is rounded up to `ATTN_SCRATCH_ALIGN`. This is *sufficiency*, not slack: the
  measured high-water mark equals the reported size at every D tested (slack 0). The rounding
  exists so the result is a legal C11 `aligned_alloc` size (size % alignment == 0) and so an
  array of scratches stays aligned.
